/**
 * @file
 * Protected Content behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.procProc = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
